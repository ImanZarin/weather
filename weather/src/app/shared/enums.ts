export enum MyStorage {
  unit = 'UNIT',
  selectedCity = 'SELECTEDCITY',
}

export enum MyUnits {
  metric = 'metric',
  imperial = 'imperial',
}

export enum MyRoute {
  weather = '/weather',
  search = '/search',
}
